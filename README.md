# OrderSapp Bot

Simple WhatsApp bot using Baileys.

## Run
1) Install deps: `bun install`
2) Start: `bun start`
3) Scan the QR code in terminal with WhatsApp.

## Test
Send `ping` to the bot; it replies with `pong`.
